{
  "name": "latestarr",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "changeset": "changeset",
    "version-packages": "node scripts/write-changelog.mjs && changeset version && node scripts/sync-root-version.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0",
    "prettier": "^3.3.0",
    "turbo": "^2.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ],
    "overrides": {
      "esbuild": "^0.25.0"
    }
  }
}
